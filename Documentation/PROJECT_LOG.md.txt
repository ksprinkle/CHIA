# Community Health Intelligence Atlas (CHIA)

# Project Log

---

# Version 0.1.0 — Project Foundation

**Date:** August 28, 2026

## Project Established

The Community Health Intelligence Atlas (CHIA) project was created to explore geographic patterns of health burden across the United States using authoritative public datasets.

The project began after reviewing CDC research showing that chronic diseases frequently cluster geographically and are closely associated with socioeconomic and healthcare-access factors. Rather than focusing on a single disease, the project's objective became understanding the combination of characteristics associated with overall community health burden.

---

## Major Decisions

### Project Scope

Version 1 will focus exclusively on the United States.

The initial geographic unit of analysis will be counties, with the potential to incorporate ZIP Code Tabulation Areas or census tracts in future versions if doing so materially improves the analysis.

---

### Project Purpose

CHIA is a research and decision-support tool.

It is **not** intended to diagnose individuals, predict personal health outcomes, or provide medical advice.

---

### Development Philosophy

The project will emphasize:

* transparency
* reproducibility
* explainable methods
* maintainability
* practical usefulness

Complexity will only be added when it produces meaningful analytical value.

---

### Technical Direction

Version 1 will remain fully offline after data acquisition.

The finished deliverables should require no server infrastructure, cloud hosting, user accounts, or ongoing maintenance.

---

## Initial Deliverables

The following foundational documents have been created:

* Executive Report (Foundation)
* Methodology
* Data Dictionary
* Project Principles
* Project Log

Additional foundational documents will be added before implementation begins.

---

## Project Vision

The long-term vision is an interactive research atlas that helps answer questions such as:

* Which communities experience the greatest overall health burden?
* Which characteristics consistently accompany high health burden?
* Which communities demonstrate unusual resilience despite socioeconomic disadvantage?
* Which communities may benefit most from targeted interventions?

The atlas is intended to provide insight rather than definitive explanations.

---

## Version 1 Success Criteria

Version 1 will be considered complete when it includes:

* A documented methodology
* A transparent Health Burden Index
* County-level rankings
* Community comparison capability
* Similar county identification
* A downloadable Excel workbook
* A static offline HTML atlas
* Supporting documentation
* A complete data dictionary

No additional features are required for Version 1.

---

## Lessons Learned

The project's original concept centered on identifying the "sickest places in America."

Early research demonstrated that a more valuable objective is understanding the combinations of health, community, and healthcare characteristics that consistently occur together.

This shift transformed CHIA from a simple ranking exercise into a broader community health intelligence framework.

---

## Next Milestone

**Version 0.2.0 — Foundation Complete**

Planned work:

* Project Charter
* Data Sources
* Health Burden Index Specification
* Community Vulnerability Index
* Healthcare Access Index
* System architecture for the offline atlas

---

# Version 0.2.0 — County Explorer v0.2 UX: Geographic Foundation (CE-E01)

**Date:** September 4, 2026

## Scope

CE-E01 established the geographic data foundation required for the future County Explorer map-first UX (U.S. map → state selection → county selection → county profile), without building or redesigning any map UI. This slice is strictly a data-foundation slice, per the approved County Explorer UX v0.2 implementation specification (sections 10, 11, 13, 14).

## Source

U.S. Census Bureau Cartographic Boundary Files, 2025 vintage (state 1:20,000,000; county 1:500,000). Public domain. Full provenance recorded in `Documentation/GEOGRAPHIC_DATA_SOURCES.md.txt`.

## Generated Assets

* `Data/Model/geo/us-states.topojson` — national state boundaries, all 51 CHIA state-equivalents.
* `Data/Model/geo/counties/<state_fips>.topojson` — county boundaries, one file per state (51 files).
* Verbatim copies committed at `frontend/public/geo/` for frontend consumption.
* Produced by `Data/Model/convert_v02_geographic_boundaries.py`, using mapshaper as a build-time-only tool (invoked via `npx`; not added to `requirements.txt` or `frontend/package.json`).

## Validation

`Data/Model/validate_v02_geographic_join.py` confirmed: all 3,143 CHIA counties resolve to a matching Census GEOID with zero duplicates; all 51 CHIA states represented; zero state-FIPS mismatches; zero empty geometries; all 9 Connecticut planning regions verified; representative county-equivalent types (DC, Alaska boroughs/census areas, Virginia independent cities, Missouri independent city) verified.

## Tests

`tests/test_ce_e01_geographic_foundation.py` added (12 tests). Full existing CE-A00..D01 regression suite re-run and confirmed unaffected.

## Status

Geographic foundation complete. No database, schema, API, or analytical change. No map UI implemented. County Explorer visualization/map UX remains a future slice (CE-E02+).

---

# Version 0.2.0 — County Explorer v0.2 UX: U.S. Map Landing (CE-E02)

**Date:** September 4, 2026

## Scope

Replaced the text-first landing state with the U.S. map as the primary navigation surface (specification §4, §14 CE-E02). `HomePage` now renders `UsStateMap` (the CE-E01 committed `us-states.topojson`, rendered with `react-simple-maps`) alongside `StateSelect`, the required accessible non-map alternative (§9.2). Both derive their state list from a single `deriveStates(directory.counties)` helper (`lib/states.ts`), so the map and the selector can never disagree about which states exist. Added the `/states/:stateFips` route. The existing global `CountySelector` remains on every route.

## Dependencies

`react-simple-maps` 3.0.0 (runtime) and `@types/react-simple-maps` 3.0.6 (dev) added — the only new frontend dependency in the CE-E series. No backend/API/database/analytical change.

## Tests

`UsStateMap`, `StateSelect`, `StatePage`, and `states` derivation tests added; routing tests extended for map↔selector URL parity. Test fetch harness fixed to return genuine `Promise<Response>` (react-simple-maps chains `fetch(url).then(...)`).

## Status

Complete. Gate met: a user can reliably select any supported state via the map or the accessible selector.

---

# Version 0.2.0 — County Explorer v0.2 UX: State County Map (CE-E03)

**Date:** September 4, 2026

## Scope

After state selection, `StatePage` renders `StateCountyMap` (the CE-E01 committed per-state `counties/<state_fips>.topojson`) plus `CountySelectForState`, a dedicated state-scoped accessible county selector (§5, §14 CE-E03). Both consume a single `deriveCountiesForState(directory.counties, stateFips)` helper (`lib/counties.ts`). County names are presented with state context already established, resolving the duplicate-county-name problem. The map is fit to the selected state's counties using only `react-simple-maps`' own `path` — no new projection/geometry dependency. Geometry↔directory joins are by FIPS, never by name. No new route.

## Tests

`StateCountyMap`, `CountySelectForState`, and `deriveCountiesForState` tests added; routing tests extended for county map↔selector parity. No new dependency.

## Status

Complete. Gate met: a user can select any supported county within the selected state.

---

# Version 0.2.0 — County Explorer v0.2 UX: Visual County Profile (CE-E04)

**Date:** September 4, 2026

## Scope

Redesigned the text-heavy County Profile (§6, §8, §14 CE-E04) around a visual hierarchy: geographic breadcrumb (`United States → State → County`), county identity header, a compact four-dimension "Healthcare access snapshot", the access-dimension detail, the CE-C05 "What Stands Out?" interpretation, the clearly-labelled experimental composite, and methodology/provenance moved behind collapsed native `<details>` disclosures (progressive disclosure, §6.10/§6.11/§8). Added a display-only misleading-rounding fix for the experimental composite (a stored ~0.45 no longer renders as a bare "0"; §6.12). No analytical logic, interpretation rules, or API payloads changed.

## Tests

`CountyPage` test suite extended (breadcrumb, snapshot, disclosure collapse, composite rounding); two pre-existing CE-C03 assertions rescoped to the dimensions region due to legitimate new duplicate text. No new dependency.

## Status

Complete. Gate met: county results are understandable without reading large blocks of text.

---

# Version 0.2.0 — County Explorer v0.2 UX: Visualization Layer (CE-E05)

**Date:** September 4, 2026

## Scope

Added per-dimension visual indicators to the snapshot (§7, §12, §14 CE-E05): a fixed-size position **marker on a 0–100 track** for the three percentile dimensions (`PercentileIndicator`) and a visually distinct **filled coverage bar** for MUA/P (`CoverageIndicator`) — never the same widget or scale, so MUA/P geographic coverage is never implied to be a percentile. Indicators are `aria-hidden` decoration; the existing numeric value + unit text remains the accessible source of truth. A genuine 0th percentile stays visible (the marker is a fixed-size element regardless of position); unavailable dimensions render no indicator. No charting library was added. Placement confined to the snapshot; `DimensionCard` left intact.

## Tests

`CountyPage` suite extended with a CE-E05 block (indicator presence, boundary positions 0%/100%, percentile-vs-coverage widget distinction, decorative/aria-hidden, zero-vs-missing). No new dependency.

## Status

Complete. Gate met: visualizations accurately represent the underlying values and introduce no misleading comparisons.

---

# Version 0.2.0 — County Explorer v0.2 UX: Measure Exploration & Drill-Down (CE-E06)

**Date:** September 4, 2026

## Scope

Made each dimension progressively investigable (§6.7, §8.3, §14 CE-E06). The dimension Result (name, description, score, qualifier, CE-E05 indicator) stays visible; the analytical trail — underlying/source-measure data, supporting evidence, dimension-scoped methodology (`DimensionProfile.calculation_method` + primary-measure `normalization_method`), and dimension-scoped provenance (the single source identified by `DimensionProfile.source_id`) — moves behind one independent native `<details>` per dimension (`Investigate {dimension name}`). No accordion behaviour; the `<details>` `open` attribute is the only state (no React state, route, or URL change). The page-level `MethodologyPanel` / `ProvenancePanel` remain as the comprehensive reference view. No backend/type/analytical change.

## Tests

`CountyPage` suite extended with a CE-E06 block (12 cases): per-dimension disclosure, default-visible content, evidence/methodology/provenance correctly scoped and never mixed across dimensions, independent open/close, zero/missing/percentile/coverage semantics preserved, native `<details>` accessibility. No new dependency.

## Status

Complete. Gate met: the complete analytical trail remains accessible without overwhelming the primary interface.

---

# Version 0.2.0 — County Explorer v0.2 UX: Accessibility & Responsive Validation (CE-E07)

**Date:** September 5, 2026

## Scope

Consolidated accessibility and responsive validation for the CE-E02–CE-E06 map-first redesign (§9, §14 CE-E07). This is a validation slice; its one functional change is a screen-reader **route-change announcement**: a single visually-hidden polite live region in `Layout` (`aria-live="polite"`, no `role`) that announces the geographic context of each client-side navigation ("Viewing {state}" / "Viewing {county}, {state}"), derived read-only from the URL plus the loaded county directory. Closes the §9.4 gap where SPA navigation swapped the page `<h1>` silently. Added a `.visually-hidden` CSS utility used only by that region.

## Validation

14 consolidated automated accessibility tests added in `frontend/src/accessibility.test.tsx` (map↔accessible-selector parity at both geographic levels; route-announcer behaviour incl. back navigation; CE-E06 drill-down keyboard/native-semantics; colour-independent dimension values; single-h1/main-landmark/named-region structure on all three screens). Stale "no custom keyboard handling anywhere" comment corrected (CE-E02/E03 map SVG features use an explicit `onKeyDown`). Full frontend suite 180/180 green; `npm run lint` clean; `tsc && vite build` clean; backend suite and CE-E01 geographic validator unaffected and green. Real-browser / screen-reader / on-device checks (which this environment cannot execute) are defined as an execution-ready checklist in `Documentation/CE_E07_Accessibility_Responsive_Validation.md.txt`, for CE-E08. §7 analytical-map / measure-selector / choropleth / legend / tooltip functionality is explicitly recorded as **not part of the §14 baseline** and therefore N/A for those §9 clauses.

## Status

Complete. No new dependencies, no browser-automation tooling, no backend/API/database/analytical/geographic change, no tablet redesign. Gate (accessible alternative paths provide equivalent substantive information) met at the architecture and automated-test level; real-browser/AT/device sign-off is handed to CE-E08 as a defined checklist.

---

# Version 0.2.0 — County Explorer v0.2 UX: End-to-End Acceptance (CE-E08) — v0.2 UX BASELINE

**Date:** September 5, 2026

## Scope

Final acceptance of the redesigned County Explorer (§14 CE-E08). An acceptance slice: no product behaviour added. It ratifies the CHIA County Explorer v0.2 UX baseline delivered by CE-E01–CE-E07 — the geography-first journey (§3.1): U.S. map → state → county → visual profile → dimension → evidence → underlying data → methodology/provenance, with geographic context visible and announced throughout.

## Journey verification

Added `frontend/src/e2e-journey.test.tsx` (`describe('CE-E08 end-to-end acceptance')`, 3 tests, jsdom, no new tooling): the complete journey driven entirely by map selection (U.S. map click → state, state map click → county), asserting the breadcrumb, the CE-E07 route announcement ("Viewing {county}, {state}"), a single Explorer fetch, the four-dimension snapshot, one dimension's full drill-down trail (underlying data → evidence → dimension-scoped methodology → dimension-scoped provenance, correctly not mixed with other dimensions), the retained page-level Methodology/Sources panels, and context preservation + re-announcement on backward navigation (§3.5, §3.8). 3/3 pass.

## CE-D01 analytical foundation — unchanged (verified)

- **Source (git `ce-d01..HEAD`):** the only changes under `app/`, `Data/Model/`, `tests/` since `ce-d01` are three additive CE-E01 geographic-foundation files. `app/` (API, schemas, services incl. the normalization/observation pipeline), every `Data/Model/*.py` v0.1 build/normalize/score/validate script, `schema.sql`, `seed_v01.sql`, all CE-A/B/D test suites, and `requirements.txt` are byte-for-byte unchanged. `frontend/src/lib/interpretation.ts` / `dimensions.ts` / `types.ts` unchanged since `ce-d01`.
- **Data (md5):** `Data/Model/chia_v01.sqlite` md5 `f53b3de17b627b3b02796df9de33b5a4`, identical before and after the full verification run — the analytical layer is strictly read-only.
- **Behaviour:** backend suite 122/122 OK (incl. `test_ce_a06` production byte-identical + tamper detection, `test_ce_d01` DB SHA baseline + analytical-values spot check, `test_ce_b02` read-only digest); `validate_v01_database`, `validate_v01_normalization`, `validate_v01_dimension_scores`, `validate_v01_composite`, `validate_v01_county_explorer`, and `validate_v02_geographic_join` all PASSED.

## Full regression

Frontend 183/183 (18 files); `npm run lint` clean; `tsc && vite build` clean; backend 122/122; 6 validators PASSED.

## Scope boundary

The v0.2 UX baseline is the §14 geography-first journey. §3.2 measure-first journey, §3.3 county search, §3.6 change-measure-in-place, and §3.7 a dedicated reset control are **not part of the v0.2 §14 scope** (they depend on the §7 analytical-map / measure-selector capability, which §14 never scheduled and CE-E02–E07 did not build) — candidate v0.3 roadmap items.

## Residual

The CE-E07 manual browser / screen-reader / touch-device / reduced-motion checklist (`Documentation/CE_E07_Accessibility_Responsive_Validation.md.txt` §6) could not be executed in this environment (no browser automation, no AT, no devices) and is **not** represented as passed. It is carried forward as an explicit, itemised residual in `Documentation/CE_E08_EndToEnd_Acceptance.md.txt` §6 for a human validator against a running build.

## Status

**CHIA County Explorer v0.2 UX baseline established.** Journey acceptance complete; CE-D01 analytical foundation verified unchanged; backward compatibility (§13.11) verified; full regression green. Gate met at the automated-acceptance + code-audit level, with the CE-E07 manual checklist as a documented residual. No application source, backend, API, schema, database, analytical, geographic, or dependency change in CE-E08. Full acceptance record: `Documentation/CE_E08_EndToEnd_Acceptance.md.txt`.

---

*This log serves as the historical record of significant project decisions, milestones, and direction. It documents not only what changed, but why those decisions were made.*
